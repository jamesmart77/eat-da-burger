<li>
	<div class="burger-item">
		{{burger_name}}

		<button class="btn btn-warning change-devour float-right" data-id="{{id}}" data-newdevour="{{devour}}">
			{{#if devour}}DEVOUR{{/if}}
		</button>
	</div>
</li>